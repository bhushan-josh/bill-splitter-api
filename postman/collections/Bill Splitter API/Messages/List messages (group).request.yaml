$kind: http-request
description: Newest first. For a friend chat use context_type=friend & friend_id.
url: "{{baseUrl}}/api/v1/messages?context_type=group&group_id={{groupId}}&page=1&limit=25"
method: GET
queryParams:
  context_type: group
  group_id: "{{groupId}}"
  page: "1"
  limit: "25"
order: 1000
